import { GuardiansScreen } from "./screen";

/** The Guardians route (Sprint 4). Permission-gated client-side (guardian.read); API enforced. */
export default function GuardiansPage() {
  return <GuardiansScreen />;
}
