import { TeacherDetailScreen } from "./screen";

/** The dedicated teacher workspace route — profile, calendar, salary & reports. */
export default function TeacherDetailRoute() {
  return <TeacherDetailScreen />;
}
