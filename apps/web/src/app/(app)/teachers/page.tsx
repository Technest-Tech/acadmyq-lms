import { TeachersScreen } from "./screen";

/** The Teachers route (Sprint 4). Permission-gated client-side (teacher.read); API enforced. */
export default function TeachersPage() {
  return <TeachersScreen />;
}
