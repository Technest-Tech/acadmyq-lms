import { FinanceClientsScreen } from "./screen";

/** Super Admin → Finance → Clients: who pays the owner. Not linked to platform accounts. */
export default function AdminFinanceClientsPage() {
  return <FinanceClientsScreen />;
}
