import { FinancePaymentsScreen } from "./screen";

/** Super Admin → Finance → Payments: every unit of money received, as one ledger. */
export default function AdminFinancePaymentsPage() {
  return <FinancePaymentsScreen />;
}
